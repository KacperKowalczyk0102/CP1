arr1 = ["water", "book", "sky"]
arr2 = ["water", "book", "sky"]

def compare(arr1,arr2):
    if len(arr2) == len(arr1):
        for index in range(len(arr1)):
            if not arr1
        return True
    else:
        return False